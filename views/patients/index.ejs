<!DOCTYPE html>
<html lang="en">
<%-include("../partials/head.ejs")%>
<body>
    <%-include("../partials/header.ejs")%>
    <main>
        <div>
            <% for(let patient of patients){ %>
                <article>
                    <a href="/<%=patient._id%>">
                    <h2>
                        <%-patient.name %> - <%= patient.testCompleted ? "Test Results Available" : "Testing in Progress"%>
                    </h2>
                    <h3>
                        <%=patient.test%>
                    </h3>
                    </a>
                </article>

            <%}%>
        </div>
    </main>

</body>
</html>