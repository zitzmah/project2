<!DOCTYPE html>
<html lang="en">
<%-include("../partials/head.ejs")%>
<body>
    <%-include("../partials/header.ejs")%>
    <a href="/new"><button>Add a New Patient</button></a>
    <main>
        <div>
            <% for(let patient of patients){ %>
                <article>
                    <a href="/<%=patient._id%>">
                    <h2>
                        <%=patient.name %> <br> <%=patient.dateOfBirth%>
                    </h2>
                    <h3>
                        <%=patient.test%> - <%= patient.testCompleted ? "Test Completed" : "Testing in Progress"%>
                        <%if(patient.testCompleted) {%>
                            <br> Culture Identification: <%=patient.bacteriaIdentification%>
                        <%}%>
                    </h3>
                    </a>
                </article>

            <%}%>
        </div>
    </main>

</body>
</html>